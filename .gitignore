# Local config with real credentials — NEVER commit
backend/src/main/resources/application-local.yml

# Build outputs
backend/build/
backend/.gradle/
*.jar
*.war

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Flutter
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies

# Node / admin frontend
admin-frontend/node_modules/
admin-frontend/dist/
admin-frontend/.env
admin-frontend/.env.local

# Env files with secrets
*.env
.env
.env.*
# Allow production env (no secrets — just the public API URL)
!admin-frontend/.env.production
